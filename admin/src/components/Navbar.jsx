import { assets } from '../assets/assets';

const Navbar = () => {
  return (
    <div>
      <div>
        <img src={assets.admin_logo} alt='logo pic' />
        <p></p>
      </div>
    </div>
  );
};

export default Navbar;
